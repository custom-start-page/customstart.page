const logger = require('../logger');
const app = require('../app');

module.exports = function() {
    // Render startpage.
    // app.get('/:page', function(req, res) {
    //     logger.info(req.params.page);

    //     res.render('index', {
    //         layout: 'common',
    //         relativeUrl: '',
    //     });
    // });
};
