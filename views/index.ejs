<section class="container">
    <p class="text-center">pick a start page and start customising</p>
    <div class="row">
        <% for (let theme of themes) { %>
            <div class="col-lg-4">
                <div class="panel panel-default">
                    <a href="//<%= theme.slug %>.<%- config.domain %>:/preview">
                        <div class="panel-heading">
                            <h2 class="panel-title"><%= theme.name %></h2>
                        </div>
                        <div class="panel-image-container">
                            <img width="100%" src="//<%= theme.slug %>.<%- config.domain %>:/manifest/<%= theme.preview %>" />
                            <ul class="pills">
                                <% theme.features.forEach(feature => { %>
                                    <li><%= feature %></li>
                                <% }) %>
                            </ul>
                        </div>
                    </a>
                    <div class="panel-footer">
                        by <a href="<%= theme.author.link %>" target="_blank"><%= theme.author.name %></a>
                    </div>
                </div>
                </a>
            </div>
        <% } %>
    </div>
</section>
<section class="jumbotron">
    <div class="container">
        <div class="row text-center">
            <div class="col-sm-6 col-sm-push-3">
                <h2>What is a startpage?</h2>
                <p>It's the first page you see when you open your browser or a new tab. It might just be the homepage your browser has as default. Though if you want something a little more unique, you can use and customise one of the ones provided here.</p>
                <hr>
                <h2>What is this site for?</h2>
                <p>We provide a collection of free to use startpages which can be customised to your needs.</p>
                <hr>
                <h2>How do I set set a startpage as my default?</h2>
                <p>This depends on your browser. Find out <a href="/how-to-set-my-startpage">here</a>.</p>
            </div>
        </div>
    </div>
</section>
